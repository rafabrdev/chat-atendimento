# ========================================
# ARQUIVO DE EXEMPLO DE VARIÁVEIS DE AMBIENTE
# ========================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env.local
# 2. Preencha com os valores reais (NUNCA commite o .env.local!)
# 3. Use os scripts deploy-staging.ps1 ou deploy-production.ps1
#
# IMPORTANTE: NUNCA coloque senhas reais neste arquivo .env.example!
# Este arquivo DEVE ser commitado no repositório como documentação.

# ========================================
# CONFIGURAÇÕES GERAIS
# ========================================

# Endereço IP do servidor EC2
EC2_HOST=52.90.17.204

# Caminho para a chave SSH
SSH_KEY_PATH=~/.ssh/chat-atendimento-new-key.pem

# Secret JWT (use uma string forte e aleatória)
JWT_SECRET=your-super-secret-jwt-key-here-change-this

# ========================================
# CONFIGURAÇÕES DO MONGODB
# ========================================

# MongoDB URI para Staging
# Formato: mongodb+srv://usuario:senha@cluster.mongodb.net/database
MONGODB_URI_STAGING=mongodb+srv://username:password@cluster.mongodb.net/chat-staging?retryWrites=true&w=majority

# MongoDB URI para Produção
MONGODB_URI_PRODUCTION=mongodb+srv://username:password@cluster.mongodb.net/chat-production?retryWrites=true&w=majority

# ========================================
# CONFIGURAÇÕES AWS
# ========================================

# Credenciais AWS (se não estiverem configuradas via AWS CLI)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# Buckets S3
S3_BUCKET_NAME_STAGING=chat-atendimento-staging
S3_BUCKET_NAME_PRODUCTION=chat-atendimento-production

# ========================================
# CONFIGURAÇÕES DO GITHUB ACTIONS
# ========================================
# Estas devem ser configuradas como Secrets no GitHub:
# 
# Vá em: Settings > Secrets and variables > Actions
# 
# Secrets necessários:
# - EC2_HOST: IP do servidor EC2
# - EC2_USER: Usuário SSH (geralmente 'ec2-user')
# - EC2_SSH_KEY: Conteúdo completo da chave privada SSH
# - MONGODB_URI: URI do MongoDB para staging
# - MONGODB_URI_PROD: URI do MongoDB para produção
# - JWT_SECRET: Secret para tokens JWT
# - AWS_ACCESS_KEY_ID: ID da chave de acesso AWS
# - AWS_SECRET_ACCESS_KEY: Chave secreta AWS
# - S3_BUCKET_NAME_STAGING: Nome do bucket S3 para staging
# - S3_BUCKET_NAME_PRODUCTION: Nome do bucket S3 para produção
# - GITHUB_TOKEN: Automaticamente disponível no GitHub Actions

# ========================================
# NOTAS DE SEGURANÇA
# ========================================
# 
# 1. NUNCA commite arquivos .env com dados reais
# 2. Adicione .env.local ao .gitignore
# 3. Use senhas fortes e únicas para cada ambiente
# 4. Rotacione as credenciais regularmente
# 5. Use IAM roles com permissões mínimas necessárias
# 6. Configure backups automáticos do MongoDB
# 7. Monitore logs de acesso e atividades suspeitas
